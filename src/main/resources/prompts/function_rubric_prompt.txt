You are a SQL Server expert generating a grading rubric for a FUNCTION question.
Return only valid JSON. Do not use markdown.

=== QUESTION ===
Content: %s
Reference SQL (correctQuery): %s
Routine Type: %s
Total Points: %.2f

=== CRITICAL PRINCIPLES (MUST READ) ===
DO NOT guess expected_result or expected_value. The engine will run the reference answer in a sandbox
and capture the actual result as expected_value. You ONLY generate "scenarios":
setup_script + invocation_query + validation_query.

**IMPORTANT - READ DDL SCHEMA CONTEXT CAREFULLY**:
- You will receive DDL schema context at the end of this prompt (if available)
- MUST read DDL carefully to know EXACTLY:
  + Table names (e.g., NhanVien, PhongBan, ChuyenXe)
  + Column names (e.g., PhongBan not MaPhongBan, Luong not Salary)
  + Data types (VARCHAR, INT, DECIMAL, DATETIME, NVARCHAR, ...)
  + **COLUMN LENGTH**: VARCHAR(10) can only hold max 10 characters, NVARCHAR(50) max 50 characters
  + Number of columns (so INSERT has enough VALUES)
  + Primary keys and Foreign keys
- DO NOT guess column names, DO NOT use column names not in DDL
- **DO NOT create values longer than column length**
- If no DDL context, infer structure from correctQuery

=== RUBRIC REQUIREMENTS ===
Generate JSON rubric with this structure:

{
  "question_id": "Q_AUTO_01",
  "total_points": %.2f,
  "question_category": "ROUTINE",
  "grading_payload": {
    "grading_settings": {
      "syntax_error_action": "FAIL_ALL",
      "print_output_compare_mode": "LENIENT",
      "case_sensitive_names": false,
      "positive_only_scoring": false
    },
    "routines": [
      {
        "expected_name": "<function name from reference>",
        "expected_type": "FUNCTION",
        "expected_return_type": "<INT, DECIMAL, NVARCHAR, TABLE, etc.>",
        "parameters": [
          {
            "name": "<param name without @ prefix>",
            "expected_type": "<SQL type: INT, VARCHAR(50), DECIMAL(15,2), etc.>",
            "expected_mode": "IN"
          }
        ]
      }
    ],
    "test_cases": [
      {
        "case_id": "TC_01",
        "case_name": "<short Vietnamese description>",
        "verification_type": "RETURN_VALUE",
        "match_type": "EXACT",
        "score_weight": 0.25,
        "description": "<what this test checks>",
        "setup_script": "-- MUST use [{SCHEMA}] when referencing tables\nDELETE FROM [{SCHEMA}].ChildTable WHERE ...;\nDELETE FROM [{SCHEMA}].ParentTable WHERE ...;\nINSERT INTO [{SCHEMA}].ParentTable VALUES (...);\nINSERT INTO [{SCHEMA}].ChildTable VALUES (...);",
        "input_parameters": {},
        "invocation_query": "",
        "validation_query": "SELECT [{SCHEMA}].FunctionName('VALUE1', 123)"
      }
    ]
  }
}

=== TEST CASE CREATION RULES ===
1. COVERAGE-DRIVEN CASE COUNT:
   - Do not target a fixed number of test cases. Generate the number of non-redundant cases required by the observable branches in QUESTION and REFERENCE SQL.
   - Generate at least three independent core valid cases.
   - If there is only one core branch, exercise it with at least three meaningfully different literal inputs or database states and verify different business values when possible.
   - Do not create cosmetic duplicates that only change case_id, description, or irrelevant data.
   - Cover every materially different branch before adding lower-value edge cases. Never invent behavior that is not required by QUESTION or implemented by REFERENCE SQL.

2. FUNCTION-SPECIFIC COVERAGE:
   - For a scalar function, cover relevant conditional branches, exact thresholds, immediately-below/immediately-above boundaries, and distinct return outcomes.
   - For a scalar function that reads tables, include meaningful states such as no matching row, one matching row, multiple matching rows, and unrelated rows when those states can distinguish incorrect implementations.
   - For aggregate or filtering logic, include data that distinguishes the intended filtered set from all rows in the table.
   - For a table-valued function, cover empty result, one-row result, multiple-row result, qualifying rows, non-qualifying rows, and unrelated rows when relevant to the reference logic.
   - Generate NULL or empty-string cases only when QUESTION or REFERENCE SQL defines meaningful behavior for them. Do not invent invalid-input requirements merely to add an edge case.
   - Use ORDER BY in table-valued validation_query whenever row order is not otherwise deterministic. Order by stable key/output columns sufficient to make comparison repeatable.

3. SCORE BALANCE:
   - When both core valid cases and boundary/edge cases exist, assign exactly 60 percent of total score_weight to core valid cases and exactly 40 percent to boundary/edge cases.
   - If no meaningful boundary or edge behavior exists, assign 100 percent to core valid cases. Do not invent edge cases merely to fill the edge pool.
   - Every generated test case must have a positive score_weight no greater than 1.0.
   - Each score_weight must use at most two decimal places. Values such as 0.25, 0.3, and 0.15 are valid; values such as 0.133333 are forbidden.
   - The final score_weight total must equal exactly 1.0. When a pool cannot be divided evenly with two decimal places, distribute the remainder to one case, for example 0.13 + 0.13 + 0.14 = 0.40.
   - Do not let numerous edge cases outweigh the function's core required behavior.

4. DATABASE SETUP:
   - If the function does not read database tables, set setup_script to an empty string and do not invent table data.
   - If the function reads database tables, every test case MUST have a complete, self-contained setup_script.
   - **IMPORTANT**: Start with DELETE to clear old data and avoid duplicate primary key errors:
     ```sql
     DELETE FROM [{SCHEMA}].ChildTable WHERE PK IN ('TC01_C1', 'TC01_C2');
     DELETE FROM [{SCHEMA}].ParentTable WHERE PK IN ('TC01_P1');
     ```
   - Or use DIFFERENT primary keys for each test case (TC_01 uses 'P001', TC_02 uses 'P002', etc.)
   - **IMPORTANT**: Check DDL schema context to know EXACT column names and count
   - **IMPORTANT**: INSERT must have enough values matching the number of columns specified
   - **IMPORTANT**: VARCHAR/NVARCHAR values must be shorter than column length (e.g., VARCHAR(10) → use 'EMP001' not 'EMPLOYEE_001')
   - For foreign keys: INSERT parent rows BEFORE child rows
   - For DELETE: DELETE child rows BEFORE parent rows
   - **IMPORTANT**: Include ALL columns needed by the function:
     * Primary key columns
     * Foreign key columns (for relationships)
     * NOT NULL columns without defaults
     * Any columns the function reads or returns (name, status, price, amount, etc.)
   - DO NOT insert only the key if function needs other columns like name, amount, or status
   - Engine wraps test case in transaction and rollback, so no cleanup needed

5. validation_query: REQUIRED. SQL SELECT that calls the function.
   - **Scalar function** (returns single value):
     ```sql
     SELECT [{SCHEMA}].FN_TongLuong('PB001')
     ```
   - **Table-valued function** (returns rows):
     ```sql
     SELECT MaNV, HoTen, Luong
     FROM [{SCHEMA}].FN_NhanVienTheoPhongBan('PB001')
     ORDER BY MaNV
     ```
   - Use literal arguments directly in validation_query
   - input_parameters is metadata only, engine does NOT convert it to SQL variables

6. DO NOT generate "expected_result"/"expected_value" — engine derives automatically

7. verification_type for Functions:
   - **RETURN_VALUE**: For scalar functions (INT, DECIMAL, VARCHAR, etc.)
   - **RESULT_SET**: For table-valued functions (returns rows)
   - DO NOT use SIDE_EFFECT, PRINT_OUTPUT, or OUT_PARAMETER for functions

=== SCHEMA REFERENCE RULES (MANDATORY) ===
- MUST use placeholder [{SCHEMA}] when referencing tables/functions in setup_script and validation_query
- NEVER use "dbo." prefix or bare unqualified names
- Example CORRECT: "INSERT INTO [{SCHEMA}].NhanVien VALUES (...)"
- Example WRONG:  "INSERT INTO NhanVien VALUES (...)"   ← no schema
- Example WRONG:  "INSERT INTO dbo.NhanVien VALUES (...)" ← engine will look in wrong place

=== IMPORTANT NOTES ===
- routines[] must list ONLY the function(s) the QUESTION explicitly asks student to create
- DO NOT list helper functions used internally by reference solution
- DO NOT use CREATE, ALTER, DROP, CREATE FUNCTION, or GO in setup_script
- DO NOT invent tables or columns not in DDL schema context or reference SQL
- Database: Microsoft SQL Server (MSSQL / T-SQL)
- **READ DDL SCHEMA CONTEXT below carefully for exact table names, column names, data types, and column lengths**

=== DDL SCHEMA CONTEXT ===
%s

=== REQUIREMENTS ===
Based on DDL schema context above, create test cases with:
- EXACT table names from DDL
- EXACT column names from DDL (do not guess, do not use different names)
- Number of VALUES matching number of columns in INSERT INTO
- Correct data types (VARCHAR use '', INT no quotes, DATETIME use '', NVARCHAR use N'')
- **Appropriate value lengths**: VARCHAR(10) → use values ≤ 10 chars (e.g., 'EMP001' OK, 'EMPLOYEE_001' WRONG)
- Include ALL columns the function needs (not just primary key)

=== PRE-FLIGHT CHECK BEFORE RETURNING JSON ===
- Return one complete, parseable JSON object with all arrays and objects properly closed.
- routines[] contains only the function(s) explicitly required by QUESTION and preserves the exact required metadata contract.
- test_cases[] contains at least three independent core valid cases.
- Every materially different observable branch in QUESTION and REFERENCE SQL is covered by a non-redundant case.
- Every scalar case uses RETURN_VALUE; every table-valued case uses RESULT_SET; no function case uses SIDE_EFFECT, PRINT_OUTPUT, or OUT_PARAMETER.
- Every non-empty setup_script is self-contained, respects DDL types/lengths/FKs, and uses [{SCHEMA}] for all table references.
- Every validation_query calls the required function through [{SCHEMA}], uses literal arguments, and returns deterministic output.
- Table-valued validation_query uses ORDER BY whenever comparison would otherwise depend on row order.
- Every score_weight has at most two decimal places, all weights are positive, core/edge pools follow the required balance, and the final total is exactly 1.0.
- The JSON contains no expected_result, expected_value, markdown, comments outside SQL strings, or extra explanation.

Return EXACT JSON FORMAT (no markdown, no extra explanation):
